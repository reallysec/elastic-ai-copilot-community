# Build-time-only dependencies — used by the `compiler` stage of the root
# Dockerfile to Cython-compile the license-critical backend modules into
# native .so extensions. These are NOT installed into the runtime image.
#
# See poc/backend/_cython_build.py and docs/SECURITY.md
# ("License enforcement integrity").
#
# A C toolchain must also be present on the build host (the Dockerfile
# installs build-essential; python:3.13-slim already ships the CPython
# development headers).
cython>=3.0,<4.0
setuptools>=68
wheel
