fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.11
openai>=1.50
elasticsearch[async]>=8.13,<9
python-dotenv>=1.0
PyYAML>=6.0
httpx>=0.27
cryptography>=42
prometheus-client>=0.20
# Enterprise user table (backend/user_db.py). Only imported when
# RST_USER_DB_URL is set; a single-account deployment never touches it.
psycopg[binary]>=3.2
