"""资产 / 身份富化 —— 界面上那一页叫「资产与身份」（`/asset-identity`）。

四个词是同一件事，写在这里省得每次重新对一遍：

  * 路由 `/asset-identity`、页面 `AssetIdentityPage` —— 用户看到的那一页；
  * 接口 `/api/admin/enrichment/*` —— 那一页的数据来源；
  * 这个包 `enrich/` —— 富化本身：把一条日志里的 IP / 主机名 / 账号解析成
    一个资产，再把资产的属性挂回去。

名字不统一是历史：先有的富化管线，后有的页面。路由是对外契约不能改，而
包名和接口路径改一次要动几十处、还要跨版本兼容，收益抵不过风险 —— 所以
留着，但在这里说清楚它们是同一条链路。
"""
